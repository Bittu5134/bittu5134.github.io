---
layout: layouts/base.njk
---
{% import "macros/icons.njk" as icons %}

<div class="min-h-screen bg-[#f6eedb] retro-dots-bg text-[#14161f] font-sans selection:bg-[#fde047] selection:text-black">
  {% include "components/retro-header.njk" %}

  <main class="max-w-4xl mx-auto px-4 sm:px-6 py-8 sm:py-12">
    <!-- Navigation & Share -->
    <div class="flex items-center justify-between gap-4 mb-8">
      <a
        href="/blog"
        class="btn-brutal-white px-3.5 py-1.5 text-xs sm:text-sm"
      >
        {{ icons.arrowLeft("w-4 h-4") }}
        <span>ALL ARTICLES</span>
      </a>

      <button
        id="share-article-btn"
        class="btn-brutal-white px-3.5 py-1.5 text-xs sm:text-sm hover:bg-[#86efac]"
      >
        <span class="share-default flex items-center gap-1.5">
          {{ icons.share2("w-3.5 h-3.5") }}
          <span>SHARE ARTICLE</span>
        </span>
        <span class="share-copied hidden flex items-center gap-1.5 text-emerald-800 font-bold">
          {{ icons.check("w-3.5 h-3.5 text-emerald-800 stroke-[3]") }}
          <span>LINK COPIED!</span>
        </span>
      </button>
    </div>

    <!-- Article Box -->
    <article class="p-6 sm:p-10 md:p-12 card-brutal shadow-brutal-lg">
      <!-- Metadata Header -->
      <div class="mb-8 pb-6 border-b-[3px] border-black">
        <div class="flex flex-wrap items-center gap-2 mb-4 font-mono text-xs sm:text-sm font-bold">
          <span class="px-2.5 py-1 bg-[#86efac] border border-black">
            STAMP: {{ date | upper }}
          </span>
          <span class="px-2.5 py-1 bg-[#fde047] border border-black">
            {{ readTime | upper }}
          </span>
        </div>

        <h1 class="text-3xl sm:text-4xl md:text-5xl font-black text-black leading-tight mb-4">
          {{ title }}
        </h1>

        <p class="font-mono text-sm sm:text-base text-black/80 font-bold leading-relaxed mb-4">
          {{ summary }}
        </p>

        <div class="flex flex-wrap gap-1.5">
          {% for tag in tags %}
            {% if tag != "posts" %}
              <span class="tag-pill">
                #{{ tag | upper }}
              </span>
            {% endif %}
          {% endfor %}
        </div>
      </div>

      <!-- Markdown Prose Content -->
      <div class="prose-retro">
        {{ content | safe }}
      </div>

      <!-- Post Footer -->
      <div class="mt-12 pt-6 border-t-[3px] border-black flex flex-col sm:flex-row items-center justify-between gap-4 font-mono text-xs sm:text-sm font-bold">
        <a
          href="/blog"
          class="btn-brutal-white px-3.5 py-1.5 text-xs sm:text-sm"
        >
          {{ icons.arrowLeft("w-4 h-4") }}
          <span>BACK TO BLOG DIRECTORY</span>
        </a>

        <a
          href="https://github.com/Bittu5134"
          target="_blank"
          rel="noopener noreferrer"
          class="text-black/70 hover:text-black hover:underline flex items-center gap-1"
        >
          {{ icons.github("w-4 h-4 text-black") }}
          <span>GITHUB: @Bittu5134</span>
        </a>
      </div>
    </article>
  </main>
</div>
